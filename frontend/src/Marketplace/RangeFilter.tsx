import React from 'react';

interface RangeFilterProps {
    min: string,
    max: string,
    setRange: any, // modify state of Marketplace so that new listings are fetched and marketplace is re-rendered
}

function RangeFilter(props: RangeFilterProps) {
    return (
        <div>

        </div>
    );
}

export default RangeFilter;